// 2: Escribir un programa que reciba como argumento un conjunto de números. Calcular la media mediante una función. 
#include <stdio.h>
{
   int n1, n2, n3, n4, n5, suma=0;
    float = div=0 
    media (n1, n2, n3, n4, n5, suma, div);
    return 0
}
void media (int n1, int n2, int n3, int n4, int n5, int suma, float div)
{
    printf ("Escriba el primer número: "); scanf ("%d", &n1);
    printf ("Escriba el segundo número: "); scanf ("%d", &n2);
    printf ("Escriba el tercer número: "); scanf ("%d", &n3);
    printf ("Escriba el cuarto número: "); scanf ("%d", n4);
    printf ("Escriba el último número: "); scanf ("%d", n5);
    {
    suma = n1 + n2 + n3 + n4 + n5;
    div = suma / 5;
    }

    printf ("El resultado final es de: %.2f ", div);
    
}